
  # Interactive Strategy Simulation Prototype

  This is a code bundle for Interactive Strategy Simulation Prototype. The original project is available at https://www.figma.com/design/kTP0kT4omym6Mg2o0ENJoS/Interactive-Strategy-Simulation-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  